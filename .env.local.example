# Neon PostgreSQL Connection URL (e.g. postgresql://[user]:[password]@[neon-host]/[db-name]?sslmode=require)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# JWT Token secret (minimum 32 characters for security)
JWT_SECRET="aasa-med-chem-default-jwt-secret-key-32-chars"
